---
type: bug
title: "Duplicated documentation comments in task/callbacks.zig"
description: "In src/task/callbacks.zig lines 584–593, the exact same 5-line doc comment block for task_add_done_callback is duplicated verbatim back-to-back."
severity: "Low"
status: "Fixed"
file: "src/task/callbacks.zig"
timestamp: "2026-09-07T14:55:00Z"
---

# BUG-325 — Duplicated documentation comments in task/callbacks.zig

- **Status**: 🟢 Fixed (2026-09-07)
- **Severity**: 🟢 Low — Redundant duplicate doc comment block
- **Files**:
  - `src/task/callbacks.zig` (lines 584–593)
- **Lessons**: [Defensive Programming](../lessons/10-defensive-programming-and-code-quality.md)
- **Description**:
In `src/task/callbacks.zig`:
Lines 584–588 and 589–593 contain the exact same doc comments for `task_add_done_callback`:
```zig
/// Add a callback to be run when the task is done.
///
/// If context is not specified, uses the current context.
///
/// Raises InvalidStateError if the task is already done.
```
The second comment block is completely redundant.
- **Root Cause**: Copy-paste duplication during refactoring.
- **Trigger Scenario**: Code inspection.
- **Proposed Remediation**: Delete lines 589–593.

- **Fix (Applied — 2026-09-07)**:
  - Deleted the second copy of the duplicated 5-line comment block at `src/task/callbacks.zig:589-593`. (Content-drift note from validation: the duplicated block was the BUG-273 explanatory comment, not the `task_add_done_callback` doc comment quoted in the original report — that text does not exist in the file; the cited artifact and lines were accurate.)
- **Validation (2026-09-07)**: Valid in substance — the cited artifact exists at the cited lines; the quoted content does not.
  - A verbatim duplicated 5-line comment block exists at exactly `src/task/callbacks.zig:584-593` (lines 584-588 == lines 589-593).
  - Drift: the duplicated block is the `BUG-273` explanatory comment (executor `cleanup_task` ownership on error exits), not the `task_add_done_callback` doc comment quoted above — that text does not exist anywhere in the file. The redundancy itself is real; deleting the second copy remains the fix.

## OKF Metadata

- **Type**: `bug`
- **Tags**: [documentation, code-cleanliness, duplicate-comment]
