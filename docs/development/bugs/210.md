---
type: bug
title: "Datagram SendToData Buffer and Transport Reference Leak on Protocol Exception"
description: "In src/transports/datagram/write.zig, sendto_completed conditions cleanup on success flag, leaking SendToData, data buffer, and transport reference if protocol.resume_writing raises an error."
severity: "Medium"
status: "Fixed"
file: "src/transports/datagram/write.zig"
timestamp: "2026-08-16T09:08:00Z"
---

# BUG-210 — Missing Null Sentinels on CPython String APIs in src/transports/datagram/write.zig

- **Status**: ✅ Fixed
- **Severity**: 🟡 Medium — Resource Leak
- **Files**: `src/transports/datagram/write.zig:28-65`
- **Lessons**: [Memory & Refcounting §Lesson 2](../lessons/01-memory-and-reference-counting.md), [Lifecycle & Error Handling §Lesson 4](../lessons/03-event-loop-lifecycle.md)
- **Description**: In `src/transports/datagram/write.zig`, `sendto_completed` uses `var success = false; defer if (success) cleanup_sendto(sd);`. If protocol `resume_writing` lookup or invocation fails with an exception, `success` is never set to `true`, skipping `cleanup_sendto`. This leaves `sd`, `sd.buf`, and `transport` refcount leaked.
- **Trigger**: Error or exception raised by `protocol.resume_writing()`.
- **Consequences**: Leaked heap buffer, struct, and transport PyObject reference.
- **Fix**: Use unconditional `defer cleanup_sendto(@ptrCast(@alignCast(sd)));`.
