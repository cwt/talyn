---
type: bug
title: "Duplicated Address.toPyAddr logic and pseudosocket getsockname/getpeername"
description: "toPyAddr and toPyAddrWithPort duplicate 30 lines; pseudosocket duplicate 20 lines; hides fixes and bloats binary."
severity: "Low"
status: "Open"
file: "src/utils/address.zig"
timestamp: "2026-08-21T00:00:00Z"
---

# BUG-262 — Duplicated `Address.toPyAddr` logic and `pseudosocket` getsockname/getpeername

- **Status**: 🟢 Open (confirmed real — 2026-08-21)
- **Validation**: 2026-08-21 — CONFIRMED REAL (duplication, low severity). `address.zig` `toPyAddr` and `toPyAddrWithPort` are near-identical (only `self.getPort()` vs the passed `port` differ); `pseudosocket.zig` `pseudosocket_getsockname`/`getpeername` share identical `AF_UNIX` + `Address.toPyAddr` logic. Maintainability/fix-drift risk, not a correctness defect.
- **Severity**: Low — Duplicated Code / Maintainability
- **Files**:
  - `src/utils/address.zig:216-257` (`toPyAddr`) vs `259-298` (`toPyAddrWithPort`) — 35 LOC duplicated
  - `src/utils/pseudosocket.zig:19-42` (`getsockname`) vs `45-67` (`getpeername`) — 20 LOC duplicated
  - Also `set_future_exception` duplicated in `src/loop/python/io/socket/ops.zig:16`, `src/loop/python/io/client/create_connection.zig:100`, `src/loop/python/io/server/create_server.zig:60`
- **Lessons**: [Defensive Programming & Code Quality](../lessons/10-defensive-programming-and-code-quality.md)

## Description

`toPyAddr` and `toPyAddrWithPort` share identical `AF_INET`/`AF_INET6`/`AF_UNIX` branches; only `self.getPort()` vs passed `port: u16` differs. Pseudosocket `getsockname`/`getpeername` duplicate `AF_UNIX` path-length check and `Address.toPyAddr` fallback. `set_future_exception` is copy-pasted 3 times.

## Trigger

Maintenance: fixing `BUG-171` (port bounds) in `fromPyAddr` required patching `toPyAddr` family too, but only one copy was updated initially in audit history.

## Consequences

- Fixes applied to one copy silently missing in the other → regression.
- Binary bloat; two `inet_ntop` call sites to audit for `PyErr_Occurred`.

## Fix (Not Applied)

Factor `toPyAddrWithPort` to call `toPyAddr` with `self.setPort(port)` on a copy, or extract helper `formatAddress(family, hostBuf, port)`.

## OKF Metadata

- **Type**: `bug`
- **Tags**: [duplication, maintainability, address]
