---
type: bug
title: "Exception swallowing and traceback destruction in Python loop protocol callbacks"
description: "In talyn/loop.py, _dispatch_data_received, _dispatch_eof_received, _dispatch_buffer_updated, and _dispatch_connection_lost catch except Exception: and construct a new synthetic Exception('... failed'), completely discarding the original exception type, message, and stack traceback passed to call_exception_handler."
severity: "Medium"
status: "Fixed"
file: "talyn/loop.py"
timestamp: "2026-09-07T14:10:00Z"
---

# BUG-321 — Exception swallowing and traceback destruction in Python loop protocol callbacks

- **Status**: 🟢 Fixed (2026-09-07)
- **Severity**: 🟡 Medium — Destruction of real exception type and traceback in protocol dispatch error handlers
- **Files**:
  - `talyn/loop.py` (`_dispatch_data_received`, `_dispatch_eof_received`, `_dispatch_buffer_updated`, `_dispatch_connection_lost`: lines 406–456)
- **Lessons**: [Defensive Programming](../lessons/10-defensive-programming-and-code-quality.md), [Python C-API Correctness](../lessons/05-python-c-api-correctness.md)
- **Description**:
In `talyn/loop.py`:
```python
def _dispatch_data_received(self, transport, data):
    try:
        transport._protocol.data_received(data)
    except Exception:
        self.call_exception_handler({
            'message': 'Fatal error on transport protocol.data_received()',
            'exception': Exception("protocol.data_received() failed"),
            'transport': transport,
            'protocol': transport._protocol,
        })
```
By creating a fresh `Exception("protocol.data_received() failed")` rather than passing the caught exception instance, the real exception type, error message, and Python stack traceback are destroyed, preventing debugging.
- **Root Cause**: Fabricating a new generic `Exception` instance instead of capturing and forwarding the actual caught exception.
- **Trigger Scenario**: Any unhandled exception raised inside a protocol callback (`data_received`, `eof_received`, `connection_lost`).
- **Proposed Remediation**: Use `except Exception as exc:` and pass `'exception': exc` in all dispatch wrappers.

- **Fix (Applied — 2026-09-07)**:
  - All four dispatch wrappers in `talyn/loop.py` (`_dispatch_data_received`, `_dispatch_eof_received`, `_dispatch_buffer_updated`, `_dispatch_connection_lost`) now use `except Exception as exc:` and pass the CAUGHT exception in the handler context (`'exception': exc`) — the real type, message, and traceback reach `call_exception_handler`. (`_dispatch_connection_lost` binds the outer parameter as `exc` and the caught exception as `exc2` to avoid shadowing.)
- **Regression test**: `tests/test_loop_internals.py::test_dispatch_protocol_callbacks_forward_real_exception` — four raising protocol callbacks; asserts each handler context carries the original exception type, message, and a live `__traceback__`.
- **Validation (2026-09-07)**: Confirmed still present.
  - All four dispatchers (`talyn/loop.py:406-456`) use bare `except Exception:` and fabricate a synthetic `Exception("... failed")` instead of forwarding the caught exception — the real type, message, and traceback are destroyed exactly as described.
  - Cosmetic drift: the quoted message strings differ from the current ones (e.g. `"Exception in data_received callback"`); the anti-pattern itself is identical.

## OKF Metadata

- **Type**: `bug`
- **Tags**: [loop, exception-handling, traceback, debuggability, python]
