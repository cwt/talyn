---
type: bug
title: "PseudoSocket.close is not idempotent — second close hits a reassigned file descriptor"
description: "In src/utils/pseudosocket.zig pseudosocket_close unconditionally closes instance.fd with no closed flag and no fd reset; Python socket semantics make double-close routine (try/finally, transport close plus GC paths), so the second call closes whatever fd number now occupies the slot."
severity: "Medium-Low"
status: "Fixed"
file: "src/utils/pseudosocket.zig"
timestamp: "2026-08-23T00:00:00Z"
---

# BUG-291 — `PseudoSocket.close()` not idempotent

- **Status**: 🟢 Fixed (2026-08-23)
- **Severity**: 🟡 Medium-Low — Double-close / fd safety
- **Files**:
  - `src/utils/pseudosocket.zig:69-73` (`_ = std.os.linux.close(instance.fd)` unconditional, no guard/reset)
- **Lessons**: [Defensive Programming §Lesson 83](../lessons/10-defensive-programming-and-code-quality.md), [io_uring & Kernel Interaction §Lesson 110](../lessons/04-io-uring-and-kernel.md)
- **Description**: PseudoSocket mimics the stdlib socket API and is handed to user code as `sock=`. stdlib `socket.close()` is idempotent; here a second close closes an arbitrary descriptor that the kernel may have reassigned — spurious disconnects or EBADF in unrelated subsystems.
- **Trigger**: Any user pattern closing the pseudo socket twice.
- **Consequences**: Closing another connection's/socket's fd → hard-to-diagnose cross-component failures.
- **Fix (Applied — 2026-08-23)**: Exactly that — `PseudoSocketObject` gained a `closed` flag; `close()` is now guarded, sets `fd = -1` like the stdlib after closing, and the factory initializes the flag explicitly.
- **Regression test**: `tests/test_pseudosocket.py::test_pseudosocket_close_is_idempotent` — double close via a real stream transport's `get_extra_info("socket")`; asserts fileno becomes -1, the original fd is EBADF, and no other descriptor is touched.

## OKF Metadata

- **Type**: `bug`
- **Tags**: [pseudosocket, double-close]
