---
type: bug
title: "Broken / uncalled dead code `write_completed` in datagram transport"
description: "write_completed in datagram/write.zig references nonexistent struct fields on CallbackData and is never called anywhere."
severity: "Low"
status: "Open"
file: "src/transports/datagram/write.zig"
timestamp: "2026-08-15T00:08:00Z"
---

# BUG-151 — Broken / uncalled dead code `write_completed` in datagram transport

- **Status**: 🔴 Open
- **Severity**: 🟢 Low — dead code / compile error if referenced
- **Files**: `src/transports/datagram/write.zig:170-201`
- **Lessons**: [Defensive §Lesson 98](../lessons/10-defensive-programming-and-code-quality.md)
- **Description**: `fn write_completed` in `src/transports/datagram/write.zig` attempts to read `data.cancelled`, `data.io_uring_err`, `data.io_uring_res` directly as struct fields on `CallbackData` instead of invoking the method functions `data.cancelled()`, `data.io_uring_err()`, `data.io_uring_res()`. The function is completely unreferenced and dead.
- **Trigger**: None (dead code; would fail to compile if referenced).
- **Consequences**: Code bloat and maintenance confusion.
- **Proposed Fix**: Delete the unused `write_completed` function.
