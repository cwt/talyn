---
type: bug
title: "DNS resolution double-dispatch and cache invalidation race"
description: "In src/loop/dns/resolv.zig mark_resolved_and_execute_user_callbacks, there is no guard on control_data.resolved. When multiple nameservers are queried concurrently, if Server 1 resolves successfully and Server 2 finishes later with no records, Server 2 executes line 562, invoking mark_resolved_and_execute_user_callbacks again. This evicts Server 1's cached record and dispatches all user callbacks a second time, raising asyncio.InvalidStateError."
severity: "Medium"
status: "Fixed"
file: "src/loop/dns/resolv.zig"
timestamp: "2026-09-07T13:15:00Z"
---

# BUG-316 — DNS resolution double-dispatch and cache invalidation race

- **Status**: 🟢 Fixed (2026-09-07)
- **Severity**: 🟡 Medium — DNS double-dispatch race condition causing future InvalidStateError and cache eviction
- **Files**:
  - `src/loop/dns/resolv.zig` (`mark_resolved_and_execute_user_callbacks`: lines 236–261; `process_dns_response`: line 562)
- **Lessons**: [Concurrency & Thread Safety](../lessons/02-concurrency-and-thread-safety.md), [Network Protocols & IO](../lessons/06-network-protocols-and-io.md)
- **Description**:
In `src/loop/dns/resolv.zig`:
```zig
pub fn mark_resolved_and_execute_user_callbacks(self: *Resolv, server_data: *ResolvServerData) void {
    var control_data = server_data.control_data;
    control_data.resolved = true;
    ...
```
There is no `if (control_data.resolved) return;` guard.
When multiple nameservers are configured:
1. Nameserver 1 receives answers, calls `mark_resolved_and_execute_user_callbacks`, dispatches callbacks to `Soon`, and caches the record.
2. Nameserver 2 finishes shortly after with no answers. Line 562 in `process_dns_response` calls `mark_resolved_and_execute_user_callbacks(server_data)`.
3. `mark_resolved_and_execute_user_callbacks` runs again, evicts the newly cached record (`self.cache.discard(record)`), and re-dispatches the user callbacks into the `Soon` ring buffer.
4. When Python futures receive the second result completion, Python raises `asyncio.InvalidStateError`.
- **Root Cause**: Missing idempotency check on `control_data.resolved` in callback execution entry point.
- **Trigger Scenario**: Multi-nameserver DNS resolution where queries resolve at slightly different times.
- **Proposed Remediation**: Add an early return at the start of `mark_resolved_and_execute_user_callbacks`: `if (control_data.resolved) return; control_data.resolved = true;`.

- **Fix (Applied — 2026-09-07)**:
  - `mark_resolved_and_execute_user_callbacks` (`src/loop/dns/resolv.zig`) gained the filed remediation: an early-return idempotency guard (`if (control_data.resolved) return;` before the `resolved = true` flip). The entry point is now safe for any current or future caller — a second invocation can never evict the cached record or re-dispatch user callbacks whose futures already carry a result.
  - The caller-side guards found during validation (see below) remain in place as the primary defense; the new guard is the belt-and-suspenders enforcement at the callback-execution entry point itself.
  - Known residual (documented, out of scope): the `errdefer control_data.resolved = false` still re-opens the window if `Soon.dispatch` fails mid-loop after partially dispatching `user_callbacks` (the reset defers cleanup to `ControlData.release`'s unresolved path, which re-dispatches already-completed callbacks). Reachable only under ready-ring exhaustion mid-dispatch; requires a dispatch-accounting redesign to close.
- **Regression test**: Zig unit test `mark_resolved_and_execute_user_callbacks is idempotent (BUG-316)` — with `resolved` pre-set, the function must return without touching the record/queries/loop (stub fields are deliberately `undefined`; reaching them crashes the test, proving the early return).
- **Validation (2026-09-07)**: Partially valid — root cause confirmed, primary race path now mitigated. Status kept Open.
  - Confirmed: `mark_resolved_and_execute_user_callbacks` (`src/loop/dns/resolv.zig:236-261`) still has no internal `if (control_data.resolved) return;` idempotency guard; the line 562 call site is unchanged.
  - Mitigated: the described normal-path double-dispatch is blocked by caller-side guards — `process_dns_response` early-returns on `control_data.resolved` (`resolv.zig:385`), `on_query_sent` does the same (`resolv.zig:288`), and the inner re-queue checks include `!control_data.resolved` (`resolv.zig:429-499`). The event loop is single-threaded with no Python re-entry between the guard and the mark call, so two invocations can no longer both reach `mark_resolved_and_execute_user_callbacks` in the normal case.
  - Residual: `errdefer control_data.resolved = false` (`resolv.zig:239`) re-opens the window if `Soon.dispatch` fails mid-loop (`:259`) after partially dispatching `user_callbacks` — a later server event then re-marks, evicts the cached record (`record.discard()` at `:253`), and re-dispatches ALL callbacks → the exact InvalidStateError/cache-eviction failure mode. The proposed internal early-return guard remains the correct fix.

## OKF Metadata

- **Type**: `bug`
- **Tags**: [dns, race-condition, double-dispatch, cache-invalidation, invalid-state]
