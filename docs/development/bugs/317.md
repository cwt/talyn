---
type: bug
title: "Multi-nameserver redundancy broken by premature abort in process_dns_response"
description: "In src/loop/dns/resolv.zig process_dns_response, if a query to the primary nameserver encounters an io_uring error or timeout (io_uring_err != .SUCCESS), it immediately calls mark_resolved_and_execute_user_callbacks. This cancels all remaining pending queries to secondary nameservers and fails the lookup, defeating multi-nameserver failover redundancy."
severity: "Medium"
status: "Fixed"
file: "src/loop/dns/resolv.zig"
timestamp: "2026-09-07T13:25:00Z"
---

# BUG-317 — Multi-nameserver redundancy broken by premature abort in process_dns_response

- **Status**: 🟢 Fixed (2026-09-07)
- **Severity**: 🟡 Medium — DNS failover redundancy broken: single server failure immediately cancels all backup nameserver queries
- **Files**:
  - `src/loop/dns/resolv.zig` (`process_dns_response`: lines 390–395)
- **Lessons**: [Network Protocols & IO](../lessons/06-network-protocols-and-io.md), [Defensive Programming](../lessons/10-defensive-programming-and-code-quality.md)
- **Description**:
In `process_dns_response`:
```zig
if (io_uring_err != .SUCCESS) {
    log.err("resolv send/recv query failed for nameserver {s}: {s}", .{ server_data.nameserver, @tagName(io_uring_err) });
    self.mark_resolved_and_execute_user_callbacks(server_data);
    return;
}
```
When multiple nameservers are configured in `/etc/resolv.conf`, if the first nameserver times out or rejects the UDP query, line 392 immediately invokes `mark_resolved_and_execute_user_callbacks`. This marks the query resolved (failed) and cancels in-flight queries to secondary nameservers, defeating the purpose of multi-nameserver redundancy.
- **Root Cause**: Treating a single nameserver query failure as an immediate fatal error for the entire multi-server resolution batch.
- **Trigger Scenario**: Systems with multiple nameservers where the primary nameserver is unresponsive or experiencing packet loss.
- **Proposed Remediation**: Track outstanding server queries; only mark resolution failed when all nameserver queries have exhausted or failed.

- **Fix (Applied — 2026-09-07)**:
  - `process_dns_response` (`src/loop/dns/resolv.zig`): the `io_uring_err != .SUCCESS` branch no longer calls `mark_resolved_and_execute_user_callbacks` unconditionally. It now consults the BUG-317 decision helper `should_fail_resolution_on_server_finish` — true only when this server is the LAST outstanding one (`tasks_finished + 1 == queries_data.len`) and nobody has resolved — and otherwise just releases the failed server, leaving the remaining nameservers in flight. The last-server failure is still routed through the record machinery (empty results → `record.discard`), preserving the established callback error semantics for total failure.
  - Ordering note: on the finalizing path `mark_resolved` runs BEFORE `server_data.release()`, so `ControlData.release()` (which frees the arena-backed state) executes with `resolved` already true and never races the callback dispatch.
  - Limitation (unchanged): the decision reads the finishing server's own result buffers; results accumulated by a still-unreleased sibling server are not aggregated (pre-existing behavior, dominant case is a failed server holding no results).
  - `/etc/resolv.conf` is hardcoded (`dns/main.zig:59`), so a Python-level multi-nameserver failover test would require system file injection — the fix is guarded by a Zig decision-table unit test plus the full resolver suite.
- **Regression test**: Zig unit test `should_fail_resolution_on_server_finish decision table (BUG-317)` — 2-outstanding first-finish → don't fail; last-finish → fail; single-server → fail immediately (old behavior preserved); already-resolved → never fail again.
- **Validation (2026-09-07)**: Confirmed still present.
  - `process_dns_response` (`src/loop/dns/resolv.zig:390-395`): a timeout or io_uring error on ONE nameserver immediately calls `mark_resolved_and_execute_user_callbacks`, whose first action is cancelling every pending server query (`resolv.zig:241-243` → `ServerQueryData.cancel` → `CancelByFd` per nameserver, `resolv.zig:132-139`) and marking the whole lookup resolved.
  - Structure confirmed: `control_data.queries_data` holds one `ServerQueryData` per nameserver (`resolv.zig:764`, `configuration.servers.len`), so a single primary-server failure aborts all secondary nameservers — failover defeated exactly as described.

## OKF Metadata

- **Type**: `bug`
- **Tags**: [dns, failover, redundancy, fault-tolerance, multi-nameserver]
